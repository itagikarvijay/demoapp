pipeline {
    agent any
      tools {
        maven 'MAVEN363'
        jdk 'JDK8'
    }
    stages {
    	stage ("initialize") {
	 steps {
                echo 'Print'
		sh "printenv"
            }			
	}
    }
}
